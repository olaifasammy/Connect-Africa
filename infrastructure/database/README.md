# Database Infrastructure

This directory contains scripts and configurations for database management (backup, restore, and maintenance) for the Connect-Africa platform.

## Scripts
- `backup.sh`: Performs a full database backup using `pg_dump`.
- `restore.sh`: Restores the database from a backup file using `psql`.
