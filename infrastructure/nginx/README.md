# Nginx Infrastructure

This directory contains Nginx configurations for serving the Connect-Africa platform.

## Files
- `nginx.conf`: Main configuration defining upstream backends and location blocks.
