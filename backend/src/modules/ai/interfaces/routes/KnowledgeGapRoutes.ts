import { Router } from 'express';
import { KnowledgeGapController } from '../controllers/KnowledgeGapController';

const router = Router();
// router.post('/', knowledgeGapController.create);
// ... implement routes

export const KnowledgeGapRoutes = router;
