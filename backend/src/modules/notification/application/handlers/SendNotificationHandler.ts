import { SendNotificationCommand } from '../commands/SendNotificationCommand';

export class SendNotificationHandler {
  async handle(command: SendNotificationCommand): Promise<void> {
    // Handling logic
  }
}
