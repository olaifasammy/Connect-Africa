export declare class Prompt {
    readonly id: string;
    readonly name: string;
    readonly content: string;
    readonly version: number;
    constructor(id: string, name: string, content: string, version: number);
}
