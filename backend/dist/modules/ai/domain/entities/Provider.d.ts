export declare class Provider {
    readonly id: string;
    readonly name: string;
    readonly isEnabled: boolean;
    readonly priority: number;
    constructor(id: string, name: string, isEnabled: boolean, priority: number);
}
